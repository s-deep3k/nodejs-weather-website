<html>
    <head>
        <link href="/css/styles.css" rel="stylesheet" type="text/css" >
    </head>
    <body>
        <h1> {{title}} </h1>
        <p>{{>header}}</p>
        <p>{{errorMsg}}</p>
    </body>
     <footer>
        {{>footer}}
    </footer>
</html>